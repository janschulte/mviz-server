import { OptionalParseIntPipe } from './optional-parse-int.pipe';

describe('OptionalParseIntPipe', () => {
  it('should be defined', () => {
    expect(new OptionalParseIntPipe()).toBeDefined();
  });
});
